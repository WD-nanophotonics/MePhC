# MePhC Affine Architecture R7.4

This evidence bundle records the contract-locked numerical-floor adjudication for the R7.3 full typed-polygon-equivalent SqrLatt `+A/-A` representations. It uses only the six fixed controls, four fixed resolutions, q0/q1/q2, six TE bands, and the existing `/home/icy/miniconda3/envs/mp/bin/python` MPB runtime.

R7.3 remains immutable: its differential response resolved `1/5` targets and is not qualified. R7.4 does not rerun the five-amplitude response ladder, does not call TriLatt MPB, and does not authorize R8 work.

Run `run_r7_4_adjudication.py` with the MP environment, then validate with `validate_r7_4.py`. Large epsilon arrays are kept out of the repository; only compact shapes, hashes, and differences are recorded.
