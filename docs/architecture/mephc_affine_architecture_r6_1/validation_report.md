# R6.1 validation report

The corrected field unit tests pass. Both downstream realized-geometry gates
pass. SqrLatt zero-ladder reproduction and resolution-12 response pass; all
five amplitudes are represented and the raw spectra are non-identical.

TriLatt is blocked at protected baseline reproducibility. The old R6 evidence
remains untouched, the mismatch is recorded in baseline_reproduction.json and
root_cause.md, and no full TriLatt amplitude sweep was executed.

Validator status: PASS_R6_1_EVIDENCE_BUNDLE; scientific terminal status: BLOCKED_BASELINE_REPRODUCIBILITY.
