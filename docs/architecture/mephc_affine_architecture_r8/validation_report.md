# R8 Validation Report

Validation is contract-first and checks the immutable baseline freeze before response payloads. All numerical records are generated by the locked runtime and are checked for call count, resolution policy, band identity, target immutability, and terminal-state consistency.

The final report is updated after the response payload and seal records are complete.
