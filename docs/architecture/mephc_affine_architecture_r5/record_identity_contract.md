# Record identity and provenance

Persistent identity contains field kind, fingerprint, stable parameters or
sample digest, reference lattice metadata, replication, motif policy,
capability and interpolation/boundary policy. Anonymous analytic callable
identity is never used as a key. Zero and constant-affine fields use the
`legacy_global_affine` namespace when semantically equivalent.
