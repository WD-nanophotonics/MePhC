# MePhC Affine Architecture R7.3

This bundle is executed from `authoritative_contract.json`, copied byte-for-byte from the Gmail attachment. The validator loads that file first and derives the runtime and evidence expectations from it.

Only SqrLatt receives fresh MPB calls. TriLatt is a strict hold with zero fresh solver calls. Protected R6 through R7.2 evidence is historical and untouched.
